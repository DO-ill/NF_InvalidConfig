/*
 * Valid nextflow.config
 * Explicitly declares an unsupported workflow language (CWL)
 */

workflow {
    language = 'CWL'
}

/*
 * Minimal Nextflow settings to keep the file valid
 */
nextflow.enable.dsl = 2
