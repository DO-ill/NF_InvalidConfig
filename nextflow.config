/*
 * Intentionally NOT a Nextflow pipeline.
 * This config declares CWL usage to trigger
 * "unsupported language" behavior in ICA.
 */

cwl {
    enabled = true
    runner  = 'cwltool'
}

/*
 * Note:
 * - No Nextflow params
 * - No Nextflow process definitions
 * - No nextflow.enable.dsl
 */
``
